<Application x:Class="Flexi2.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:Flexi2.Views"
             xmlns:vm="clr-namespace:Flexi2.ViewModels">

    <Application.Resources>

        <!-- =========================
             THEME / STYLES
             ========================= -->

        <!-- Brushes -->
        <SolidColorBrush x:Key="Bg1Brush" Color="#070A14"/>
        <SolidColorBrush x:Key="NeonCyanBrush" Color="#00E5FF"/>
        <SolidColorBrush x:Key="NeonPurpleBrush" Color="#B400FF"/>
        <SolidColorBrush x:Key="TextDimBrush" Color="#9AA4C8"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF4D6D"/>

        <!-- Global defaults -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="Control">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- Card -->
        <Style x:Key="NeonCard" TargetType="Border">
            <Setter Property="Background" Value="#0B1024"/>
            <Setter Property="BorderBrush" Value="#1B2A55"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="16"/>
        </Style>

        <!-- Buttons (no Shadow TargetName to avoid MC4111) -->
        <Style x:Key="NeonButton" TargetType="Button">
            <Setter Property="Background" Value="#101B3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource NeonCyanBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderOptions.BitmapScalingMode" Value="HighQuality"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="Root"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="14"
                                    Padding="{TemplateBinding Padding}">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.25"/>
                                </Border.Effect>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#12204A"/>
                                <Setter TargetName="Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.55"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#0E1734"/>
                                <Setter TargetName="Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.75"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.55"/>
                                <Setter TargetName="Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.10"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NeonButtonPurple" TargetType="Button" BasedOn="{StaticResource NeonButton}">
            <Setter Property="BorderBrush" Value="{StaticResource NeonPurpleBrush}"/>
        </Style>

        <!-- TextBox (explicit template so typing always works) -->
        <Style x:Key="NeonTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0E1631"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="#334BFF"/>
            <Setter Property="SelectionOpacity" Value="0.7"/>
            <Setter Property="BorderBrush" Value="#2A3B77"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="IsReadOnly" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox -->
        <Style x:Key="NeonListBox" TargetType="ListBox">
            <Setter Property="Background" Value="#0E1631"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2A3B77"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
        </Style>

        <!-- ComboBox (fixes the default ugly white dropdown/popup) -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#0E1631"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2A3B77"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                                <DockPanel LastChildFill="True">
                                    <ToggleButton x:Name="Toggle"
                                                  DockPanel.Dock="Right"
                                                  Focusable="False"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Width="28">
                                        <TextBlock Text="▾" Foreground="{TemplateBinding Foreground}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </ToggleButton>
                                    <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      VerticalAlignment="Center"
                                                      Margin="2,0,6,0"
                                                      RecognizesAccessKey="True"/>
                                </DockPanel>
                            </Border>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#0E1631" BorderBrush="#2A3B77" BorderThickness="1" CornerRadius="6" Padding="2">
                                    <ScrollViewer SnapsToDevicePixels="True" MaxHeight="260">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="#0E1631"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#152654"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="#1B2A55"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.5"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Make controls use theme by default -->
        <Style TargetType="TextBox" BasedOn="{StaticResource NeonTextBox}"/>
        <Style TargetType="Button" BasedOn="{StaticResource NeonButton}"/>
        <Style TargetType="ListBox" BasedOn="{StaticResource NeonListBox}"/>

        <!-- =========================
             VM -> VIEW TEMPLATES
             ========================= -->

        <DataTemplate DataType="{x:Type vm:LoginViewModel}">
            <views:LoginView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AdminViewModel}">
            <views:AdminView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:FloorPlanViewModel}">
            <views:FloorPlanView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:TicketViewModel}">
            <views:TicketView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:PaymentViewModel}">
            <views:PaymentView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AdminUsersViewModel}">
            <views:AdminUsersView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AdminFloorViewModel}">
            <views:AdminFloorView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AdminMenuViewModel}">
            <views:AdminMenuView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AdminReportsViewModel}">
            <views:AdminReportsView/>
        </DataTemplate>

        <!-- POS Menu (Categories / Products) -->
        <DataTemplate DataType="{x:Type vm:CategoryViewModel}">
            <views:CategoryView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ProductViewModel}">
            <views:ProductView/>
        </DataTemplate>

    </Application.Resources>
</Application>
