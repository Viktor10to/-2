<UserControl x:Class="Flexi2.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource Bg1Brush}">
        <Border Style="{StaticResource NeonCard}" Margin="24" HorizontalAlignment="Center" VerticalAlignment="Center" Width="520">
            <StackPanel>
                <TextBlock Text="ADMIN PANEL" FontSize="26" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Управление на POS" Margin="0,6,0,0" Foreground="{StaticResource TextDimBrush}"/>

                <UniformGrid Columns="2" Margin="0,18,0,0">
                    <Button Style="{StaticResource NeonButton}" Content="Потребители" Command="{Binding OpenUsersCommand}" Margin="0,0,10,10" Height="48"/>
                    <Button Style="{StaticResource NeonButton}" Content="Маси / Зони" Command="{Binding OpenFloorCommand}" Margin="0,0,0,10" Height="48"/>
                    <Button Style="{StaticResource NeonButton}" Content="Меню" Command="{Binding OpenMenuCommand}" Margin="0,0,10,0" Height="48"/>
                    <Button Style="{StaticResource NeonButton}" Content="Отчети" Command="{Binding OpenReportsCommand}" Height="48"/>
                </UniformGrid>

                <Button Style="{StaticResource NeonButtonPurple}" Content="Изход" Command="{Binding LogoutCommand}" Margin="0,14,0,0" Height="48"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
