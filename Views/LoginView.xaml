<UserControl x:Class="Flexi2.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0A0A12">
        <Border Width="520" Padding="24" CornerRadius="16"
                Background="#141428" BorderThickness="2" BorderBrush="#00E5FF"
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="FLEXIPOS LOGIN" Foreground="#00FF9C" FontSize="26" Margin="0,0,0,16"/>
                <TextBlock Text="PIN" Foreground="#CCCCFF"/>
                <TextBox Text="{Binding Pin, UpdateSourceTrigger=PropertyChanged}" Height="40" FontSize="18"/>
                <Button Content="ENTER" Command="{Binding EnterCommand}" Margin="0,12,0,0"/>
                <Button Content="EXIT" Command="{Binding ExitCommand}" Margin="0,6,0,0"/>
                <TextBlock Text="{Binding Error}" Foreground="#FF4D4D" Margin="0,10,0,0"/>
                <TextBlock Text="Admin: 9999   POS: 1111" Foreground="#7777AA" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
