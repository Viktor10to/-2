<UserControl x:Class="Flexi2.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0B0B0F">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <!-- CATEGORIES -->
        <StackPanel Grid.Column="0">
            <TextBlock Text="КАТЕГОРИИ" Foreground="White" FontSize="18" Margin="0,0,0,10"/>
            <ItemsControl ItemsSource="{Binding Categories}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}"
                                Margin="0,5"
                                Command="{Binding DataContext.SelectCategoryCommand,
                                RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- PRODUCTS -->
        <StackPanel Grid.Column="1" Margin="20,0">
            <TextBlock Text="АРТИКУЛИ" Foreground="White" FontSize="18" Margin="0,0,0,10"/>
            <ItemsControl ItemsSource="{Binding Products}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}"
                                Margin="5"
                                Command="{Binding DataContext.AddProductCommand,
                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- ORDER -->
        <StackPanel Grid.Column="2">
            <TextBlock Text="{Binding Title}" Foreground="White" FontSize="20"/>
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="{Binding Product.Name}" Width="120" Foreground="White"/>
                            <Button Content="-" Command="{Binding DataContext.DecQtyCommand,
                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"/>
                            <TextBlock Text="{Binding Qty}" Width="30" TextAlignment="Center" Foreground="White"/>
                            <Button Content="+" Command="{Binding DataContext.IncQtyCommand,
                                RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="{Binding Total, StringFormat=Общо\: {0:F2} лв}"
                       Foreground="White" FontSize="18" Margin="0,10"/>

            <Button Content="ПОРЪЧАЙ" Command="{Binding PlaceOrderCommand}" Margin="0,5"/>
            <Button Content="ПРИКЛЮЧИ" Command="{Binding CloseBillCommand}" Margin="0,5"/>
            <Button Content="НАЗАД" Command="{Binding BackCommand}" Margin="0,5"/>
        </StackPanel>
    </Grid>
</UserControl>
