<UserControl x:Class="Flexi2.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0A0A12">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Background="#121225" BorderThickness="0,0,0,2" BorderBrush="#00E5FF">
            <Grid Margin="18,0">
                <TextBlock Text="{Binding Title}" Foreground="#00FF9C" FontSize="20" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="FLOOR" Command="{Binding OpenFloorCommand}" Width="120" Margin="0,0,12,0"/>
                    <Button Content="USERS" Command="{Binding OpenUsersCommand}" Width="120" Margin="0,0,12,0"/>
                    <Button Content="LOGOUT" Command="{Binding LogoutCommand}" Width="120"/>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding Current}"/>
    </Grid>
</UserControl>
