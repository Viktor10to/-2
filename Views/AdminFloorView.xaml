<UserControl x:Class="Flexi2.Views.AdminFloorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0A0A12" Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#181833" CornerRadius="14" BorderThickness="2" BorderBrush="#B400FF" Padding="14">
            <StackPanel>
                <TextBlock Text="Zones" Foreground="#00E5FF" FontSize="18" Margin="0,0,0,10"/>
                <TextBox Text="{Binding NewZoneName}" />
                <Button Content="ADD ZONE" Command="{Binding AddZoneCommand}"/>
                <Button Content="DELETE SELECTED ZONE" Command="{Binding DeleteZoneCommand}" Margin="0,10,0,0"/>
                <Separator Margin="0,12,0,12"/>
                <ListBox ItemsSource="{Binding Zones}" SelectedItem="{Binding SelectedZone}" DisplayMemberPath="Name"/>
            </StackPanel>
        </Border>

        <Border Grid.Column="1" Margin="16,0,0,0" Background="#181833" CornerRadius="14" BorderThickness="2" BorderBrush="#00FF9C" Padding="14">
            <StackPanel>
                <TextBlock Text="Tables in zone" Foreground="#00FF9C" FontSize="18" Margin="0,0,0,10"/>
                <TextBox Text="{Binding NewTableName}" />
                <Button Content="ADD TABLE" Command="{Binding AddTableCommand}"/>
                <Separator Margin="0,12,0,12"/>
                <ListBox ItemsSource="{Binding Tables}" DisplayMemberPath="Name">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <TextBlock Text="{Binding Name}" Foreground="White"/>
                                <Button DockPanel.Dock="Right" Content="DELETE"
                                        Command="{Binding DataContext.DeleteTableCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"/>
                            </DockPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
