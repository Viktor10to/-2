<UserControl x:Class="FlexiPOS.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:FlexiPOS.Models">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel>
            <Button Content="← Категории" Command="{Binding BackCommand}" Padding="16,10" Margin="0,0,12,0"/>
            <TextBlock Text="{Binding Title}" FontSize="26" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="0.8*"/>
            </Grid.ColumnDefinitions>

            <!-- Products -->
            <Border Background="{StaticResource BrushCard}"
                    BorderBrush="{StaticResource BrushStroke}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource RadiusLg}"
                    Padding="14"
                    Margin="0,0,16,0">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Продукти" FontSize="20" FontWeight="SemiBold" Margin="6,0,0,10"/>

                    <ItemsControl Grid.Row="1" ItemsSource="{Binding Products}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:ProductModel}">
                                <Button Width="260" Height="110" Margin="10"
                                        Background="{StaticResource BrushBg1}"
                                        Command="{Binding DataContext.AddProductCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}">
                                    <Grid Margin="10">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="SemiBold"/>
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                                            <TextBlock Text="{Binding Price, StringFormat={}{0:0.00} лв.}" Foreground="{StaticResource BrushText1}"/>
                                            <TextBlock Text="   " />
                                            <TextBlock Text="{Binding HasModifiers, StringFormat=Mods: {0}}" Foreground="{StaticResource BrushText1}"/>
                                        </StackPanel>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>

            <!-- Order summary -->
            <Border Grid.Column="1"
                    Background="{StaticResource BrushCard}"
                    BorderBrush="{StaticResource BrushStroke}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource RadiusLg}"
                    Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Сметка" FontSize="20" FontWeight="SemiBold" Margin="6,0,0,10"/>

                    <ListBox Grid.Row="1" ItemsSource="{Binding Lines}" Background="{StaticResource BrushBg1}" BorderBrush="{StaticResource BrushStroke}">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:OrderLine}">
                                <Border Margin="8" Padding="10"
                                        BorderBrush="{StaticResource BrushStroke}" BorderThickness="1"
                                        CornerRadius="{StaticResource RadiusMd}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel>
                                            <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Total, StringFormat={}{0:0.00} лв.}" Foreground="{StaticResource BrushText1}"/>
                                            <TextBlock Text="{Binding IsLocked, StringFormat=Locked: {0}}" Foreground="{StaticResource BrushText1}"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Content="−" Width="46" Height="42" Margin="0,0,8,0"
                                                    Command="{Binding DataContext.DecDraftCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}"/>
                                            <TextBlock Text="{Binding Qty}" Width="32" TextAlignment="Center" VerticalAlignment="Center"/>
                                            <Button Content="+" Width="46" Height="42" Margin="8,0,0,0"
                                                    Command="{Binding DataContext.IncDraftCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <StackPanel Grid.Row="2" Margin="0,12,0,0">
                        <Button Content="Поръчай"
                                Command="{Binding OrderCommand}"
                                Background="{StaticResource BrushAccentBlue}"
                                BorderBrush="{StaticResource BrushAccentBlue}"
                                Padding="18,12"
                                FontSize="20"/>
                        <Button Content="Приключи"
                                Command="{Binding FinishCommand}"
                                Background="{StaticResource BrushBad}"
                                BorderBrush="{StaticResource BrushBad}"
                                Padding="18,12"
                                FontSize="18"
                                Margin="0,10,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="{Binding Toast}"
                   Foreground="{StaticResource BrushText1}"
                   Margin="6,14,0,0"/>
    </Grid>
</UserControl>
