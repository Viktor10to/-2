<UserControl x:Class="Flexi2.Views.FloorPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:Flexi2.Converters"
             Background="#0B0B0F">

    <UserControl.Resources>
        <conv:TableStatusToBrushConverter x:Key="TableStatusBrush"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Border Grid.Row="0"
                Height="50"
                CornerRadius="10"
                Background="#7C3AED"
                Margin="0,0,0,20">
            <Grid>
                <TextBlock Text="FLOOR PLAN – WAITER"
                           VerticalAlignment="Center"
                           Margin="20,0,0,0"
                           Foreground="White"
                           FontSize="18"
                           FontWeight="Bold"/>

                <Button Content="LOGOUT"
                        HorizontalAlignment="Right"
                        Margin="0,0,20,0"
                        VerticalAlignment="Center"
                        Height="32"
                        Padding="20,0"
                        Background="#5B21B6"
                        Foreground="White"
                        BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- TABLES -->
        <Border Grid.Row="1"
                Background="#121212"
                CornerRadius="20"
                Padding="20">

            <ItemsControl ItemsSource="{Binding Tables}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Width="80"
                                Height="80"
                                Margin="10"
                                FontSize="20"
                                FontWeight="Bold"
                                Content="{Binding Number}"
                                Background="{Binding Status, Converter={StaticResource TableStatusBrush}}"
                                Foreground="White"
                                BorderThickness="0"
                                Command="{Binding DataContext.OpenTableCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </Border>
    </Grid>
</UserControl>
