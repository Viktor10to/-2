<UserControl x:Class="FlexiPOS.Views.FloorPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:FlexiPOS.Models">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="Floor Plan" FontSize="26" FontWeight="SemiBold"/>
            <TextBlock Text="   •   " Foreground="{StaticResource BrushText1}" Margin="8,0"/>
            <TextBlock Text="{Binding HeaderText}" Foreground="{StaticResource BrushText1}" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="310"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Zones -->
            <Border Background="{StaticResource BrushCard}"
                    BorderBrush="{StaticResource BrushStroke}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource RadiusLg}"
                    Padding="14"
                    Margin="0,0,16,0">
                <StackPanel>
                    <TextBlock Text="Зони" FontSize="20" FontWeight="SemiBold" Margin="6,0,0,12"/>

                    <ItemsControl ItemsSource="{Binding Zones}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Name}"
                                        Margin="6"
                                        Padding="14,12"
                                        Command="{Binding DataContext.SelectZoneCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Tables -->
            <Border Grid.Column="1"
                    Background="{StaticResource BrushCard}"
                    BorderBrush="{StaticResource BrushStroke}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource RadiusLg}"
                    Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="6,0,0,10">
                        <TextBlock Text="Маси" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="   (demo данни)" Foreground="{StaticResource BrushText1}" Margin="10,4,0,0"/>
                    </StackPanel>

                    <ItemsControl Grid.Row="1" ItemsSource="{Binding FilteredTables}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:TableModel}">
                                <Button Width="220" Height="140" Margin="10"
                                        Command="{Binding DataContext.OpenTableCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                        CommandParameter="{Binding}">
                                    <Border CornerRadius="{StaticResource RadiusMd}"
                                            BorderThickness="1"
                                            BorderBrush="{StaticResource BrushStroke}">
                                        <Grid Margin="12">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="SemiBold"/>

                                            <StackPanel Grid.Row="1" Margin="0,8,0,0">
                                                <TextBlock Text="{Binding CurrentTotal, StringFormat=Сметка: {0:0.00} лв.}" Foreground="{StaticResource BrushText1}"/>
                                                <TextBlock Foreground="{StaticResource BrushText1}">
                                                    <TextBlock.Text>
                                                        <MultiBinding StringFormat="Заета от: {0}">
                                                            <Binding Path="OpenedAt"/>
                                                        </MultiBinding>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>

                                            <Border Grid.Row="2" Height="10" CornerRadius="6">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource BrushGood}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="{x:Static models:TableStatus.Occupied}">
                                                                <Setter Property="Background" Value="{StaticResource BrushBad}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Grid>

        <TextBlock Grid.Row="2"
                   Text="{Binding Toast}"
                   Foreground="{StaticResource BrushBad}"
                   Margin="6,14,0,0"/>
    </Grid>
</UserControl>
