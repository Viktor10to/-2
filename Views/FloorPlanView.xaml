<UserControl x:Class="Flexi2.Views.FloorPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0A0A12">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Background="#121225" BorderThickness="0,0,0,2" BorderBrush="#00E5FF">
            <Grid Margin="18,0">
                <TextBlock Text="{Binding Title}" Foreground="#00FF9C" FontSize="20" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="MENU" Command="{Binding OpenMenuCommand}" Width="120" Margin="0,0,12,0"/>
                    <Button Content="LOGOUT" Command="{Binding LogoutCommand}" Width="120"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#0F0F1F" BorderThickness="0,0,0,1" BorderBrush="#2A2A55">
            <StackPanel Orientation="Horizontal" Margin="18,8">
                <TextBlock Text="ZONE:" Foreground="#CCCCFF" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox Width="220"
                          ItemsSource="{Binding Zones}"
                          SelectedItem="{Binding SelectedZone}"
                          DisplayMemberPath="Name"/>
            </StackPanel>
        </Border>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Tables}" Margin="20">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Command="{Binding DataContext.OpenTableCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}"
                            Width="170" Height="120" Margin="10"
                            BorderThickness="2" BorderBrush="#B400FF"
                            Background="#181833">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding Name}" Foreground="White" FontSize="18" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding Status}" Foreground="#CCCCFF" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding CurrentTotal, StringFormat={}{0:0.00} лв}" Foreground="#00FF9C" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
