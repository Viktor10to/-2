<UserControl x:Class="Flexi2.Views.FloorPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Flexi2.Models"
             Background="#0B0B0F">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{Binding Title}"
                       Foreground="White"
                       FontSize="28"
                       FontWeight="Bold"
                       DockPanel.Dock="Left"/>

            <Button Content="LOGOUT"
                    Command="{Binding LogoutCommand}"
                    Padding="18,10"
                    Background="#7C3AED"
                    Foreground="White"
                    BorderThickness="0"
                    DockPanel.Dock="Right"/>
        </DockPanel>

        <!-- FLOOR -->
        <Border Grid.Row="1"
                Background="#14141B"
                CornerRadius="20"
                Padding="10">

            <ItemsControl ItemsSource="{Binding Tables}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <!-- позициониране по X/Y -->
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:TableModel}">
                        <Button Width="90"
        Height="90"
        BorderThickness="0"
        FontSize="20"
        FontWeight="Bold"
        Foreground="White"
        PreviewMouseLeftButtonDown="Table_MouseDown"
        PreviewMouseMove="Table_MouseMove"
        PreviewMouseLeftButtonUp="Table_MouseUp">

                            <!-- статус без converter -->
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="Green"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Busy">
                                            <Setter Property="Background" Value="Red"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>

                            <TextBlock Text="{Binding Number}"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"/>
                        </Button>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </Border>
    </Grid>
</UserControl>
