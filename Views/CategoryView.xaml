<UserControl x:Class="Flexi2.Views.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#0A0A12">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Background="#121225" BorderThickness="0,0,0,2" BorderBrush="#00E5FF">
            <Grid Margin="18,0">
                <TextBlock Text="CATEGORIES" Foreground="#00FF9C" FontSize="20" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="BACK" Command="{Binding BackCommand}" Width="120" Margin="0,0,12,0"/>
                    <Button Content="LOGOUT" Command="{Binding LogoutCommand}" Width="120"/>
                </StackPanel>
            </Grid>
        </Border>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding Categories}"
                 SelectedItem="{Binding Selected}"
                 DisplayMemberPath="Name"
                 Margin="20"/>
    </Grid>
</UserControl>
